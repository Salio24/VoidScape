#pragma once
#include "Utility/HashedTag.hpp"

namespace Tags {
	CORI_DECLARE_TAG(Character);

	CORI_DECLARE_TAG(Triggers);

	CORI_DECLARE_TAG(ForTest);

	CORI_DECLARE_TAG(StaticTile);

	CORI_DECLARE_TAG(ChainCollider);
}
